{
    "signature": "function writeSourceCodeForFunction(functionDeclaration: string): string",
    "explanation": "Generates source code for the given function declaration as described, with test functions for the examples provided.",
    "examples": [
        {
            "input": "/// Adds two numbers\nfunction add(a: number, b: number): number;\nassert [1,1] == 2",
            "output": "function add(a: number, b: number): number {\n  return a + b;\n}"
        }
    ]
}